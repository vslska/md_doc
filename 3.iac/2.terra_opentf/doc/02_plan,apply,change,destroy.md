---
tags:
  - workflow
  - plan
  - apply
  - change
  - destroy
---

# Рабочий процесс Terraform: Plan, Apply, Destroy, Change

 
> **Связь с концепциями**: 
> - [[01_terraform_basics]] — основы HCL и компоненты
> - Декларативное управление состоянием (State)
> - Жизненный цикл инфраструктуры


---

## 🎯 Цели

- [ ] Освоить стандартный цикл работы: `init` → `plan` → `apply`.
- [ ] Понять, что именно делает команда `init` под капотом.
- [ ] Научиться читать вывод `plan` и различать типы изменений (`~` update vs `-/+` replace).
- [ ] Уяснить разницу между «остановкой» и «уничтожением» ресурсов в контексте IaC и экономии средств.

---

## 📚 Теория: Основной цикл (The Core Workflow)

Работа с Terraform — это не написание скрипта, который нужно «запустить». Это итеративный процесс приведения реальной инфраструктуры в соответствие с кодом.

### 1. Write / Init (Написание и Инициализация)
Ты пишешь код (`.tf` файлы) или клонируешь его из Git. Перед любыми действиями в новой директории **всегда** выполняется:

```bash
terraform init
```

**Что происходит под капотом:**
1. **Инициализация Backend**: Terraform настраивает место хранения файла состояния (`terraform.tfstate`). По умолчанию это локальный файл, но в команде это S3/Yandex Object Storage.
2. **Загрузка провайдеров**: Скачиваются бинарные файлы плагинов (например, `yandex-cloud/yandex`), указанных в блоке `required_providers`.
3. **Загрузка модулей**: Если в коде есть ссылки на внешние или локальные модули (`module "..."`), они скачиваются и подготавливаются.

> ⚠️ **Правило**: `terraform init` безопасна для повторного запуска. Если ты добавил новый провайдер или модуль в код, просто запусти её снова.

---

### 2. Plan (Планирование)
Команда для просмотра того, что Terraform *собирается* сделать, **без реального изменения инфраструктуры**.

```bash
terraform plan
```

**Зачем это нужно:**
- Проверить синтаксические и логические ошибки до применения.
- Увидеть точный список ресурсов, которые будут созданы (`+`), изменены (`~`) или удалены (`-`).
- Это твой «чек» перед оплатой: если в плане есть неожиданное удаление (`-`) важного ресурса, ты можешь отменить процесс и исправить код.

---

### 3. Apply (Применение)
Финальное выполнение плана и реальное создание/изменение инфраструктуры в облаке.

```bash
terraform apply
```

**Что происходит:**
1. Terraform автоматически выполняет скрытый `plan`, чтобы убедиться, что код не изменился с момента последнего просмотра.
2. Показывает итоговый план и запрашивает подтверждение:
   ```text
   Do you want to perform these actions?
     Terraform will perform the actions described above.
     Only 'yes' will be accepted to approve.
     Enter a value: 
   ```
3. После ввода `yes` отправляет API-запросы к провайдеру (например, Yandex Cloud) для создания ресурсов.
4. Обновляет файл `terraform.tfstate`, записывая реальные ID и атрибуты созданных ресурсов.

> 💡 **Совет**: Перед коммитом в Git всегда запускай `terraform fmt` (форматирует код в каноничный стиль) и `terraform validate` (проверяет синтаксис).

---

## 🔄 Управление изменениями (Change)

Когда ты меняешь код `.tf` и запускаешь `plan`, Terraform сравнивает желаемое состояние с текущим `state`. Изменения маркируются специальными префиксами:

| Префикс | Значение | Пример |
|---------|----------|--------|
| `+` | **Создание** нового ресурса. | Новая виртуальная машина или диск. |
| `~` | **Изменение на месте (in-place update)**. Ресурс не пересоздается, меняются только его атрибуты. | Изменение имени ВМ или добавление метки (tags). |
| `-` | **Удаление** ресурса. | Ресурс удален из кода `.tf`. |
| `-/+` | **Замена (Replace)**. Ресурс будет сначала уничтожен (`-`), а затем создан заново (`+`). | Изменение неизменяемых параметров (например, типа диска или зоны доступности). |

> ⚠️ **Внимание к `-/+`**: Если ты видишь этот префикс для критичного ресурса (например, базы данных), это означает **потерю данных** (если диск не вынесен в отдельный ресурс). Всегда читай план внимательно!

**Принудительная замена**: Если ресурс «завис» или его нужно пересоздать вручную, можно использовать флаг:
```bash
terraform apply -replace="yandex_compute_instance.vm"
```

---

## 🗑️ Уничтожение (Destroy)

Terraform управляет жизненным циклом ресурсов. В отличие от облачной консоли, где ВМ можно просто «остановить» (Stop), Terraform оперирует фактом существования ресурса. 

> 💡 **Золотое правило экономии**: Если ресурс не нужен, его нужно **удалить**, а не останавливать. Остановленная ВМ в большинстве облаков (включая Yandex Cloud) продолжает тарифицироваться за выделенные диски и зарезервированные ресурсы.

Команда для удаления всей инфраструктуры, описанной в текущей директории:
```bash
terraform destroy
```
*Также требует подтверждения `yes`. Terraform построит обратный план зависимостей (сначала удалит ВМ, потом диск, потом сеть).*

---

## Best Practices (Золотые правила Workflow)

1. **Никогда не применяй `apply` без `plan`**. В CI/CD это разделяют на два разных шага (или используют `terraform apply -auto-approve` только после успешного и проверенного `plan`).
2. **Храни State безопасно**. Никогда не коммить `terraform.tfstate` в Git. Используй удаленный бэкенд (S3, YC Object Storage) с блокировкой (state locking).
3. **Проверяй зависимости визуально**. Перед `apply` мысленно пройди по цепочке: *Диск → в той же зоне? → ВМ → в той же зоне? → Подсеть → OK*.
4. **Используй систему контроля версий (Git)**. Код Terraform — это такой же код, как и приложение. Он должен проходить code review.

---

## Итоговый чек-лист

### Понимание процесса
- [ ] Я понимаю, что `init` скачивает провайдеры и настраивает бэкенд, но не создает ресурсы.
- [ ] Я знаю, что `plan` — это безопасный режим предпросмотра.
- [ ] Я понимаю, что `apply` запрашивает подтверждение `yes` и обновляет `tfstate`.

### Чтение плана
- [ ] Я могу отличить безопасное изменение на месте (`~`) от опасного пересоздания (`-/+`).
- [ ] Я знаю, как принудительно пересоздать ресурс через флаг `-replace`.

### Управление жизненным циклом
- [ ] Я понимаю, почему для экономии средств в IaC используется `destroy`, а не «остановка» ресурсов.

---

## Главный вывод

Terraform Workflow — это цикл **предсказуемости**. Команда `plan` дает тебе возможность увидеть будущее, а `apply` делает это будущее реальностью, фиксируя его в `state`. Твоя главная задача как инженера — внимательно читать вывод `plan` и не допускать случайных `-/+` для критичных данных.

---
